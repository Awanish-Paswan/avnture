import { BlogPost } from '../models/BlogPost.js'; import { Project } from '../models/Project.js';
const staticPaths=['','/services','/services/website-development','/services/web-application-development','/services/mobile-app-development','/services/custom-software-development','/services/ui-ux-design','/services/creative-design','/work','/about','/technologies','/industries','/blog','/contact','/privacy-policy','/terms','/web-development-company-delhi','/web-development-company-lucknow','/web-development-company-gorakhpur','/web-development-company-noida','/web-development-company-gurgaon'];
export async function sitemapXml(){const base=(process.env.SITE_URL||'https://avnture.example').replace(/\/$/,'');let projects=[];let posts=[];try{[projects,posts]=await Promise.all([Project.find({status:'published'}).select('slug updatedAt').lean(),BlogPost.find({status:'published'}).select('slug updatedAt publishedAt').lean()]);}catch{}const urls=[...staticPaths.map(path=>({loc:`${base}${path}`,lastmod:null})),...projects.map(x=>({loc:`${base}/work/${x.slug}`,lastmod:x.updatedAt})),...posts.map(x=>({loc:`${base}/blog/${x.slug}`,lastmod:x.updatedAt||x.publishedAt}))];return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls.map(x=>`  <url><loc>${x.loc}</loc>${x.lastmod?`<lastmod>${new Date(x.lastmod).toISOString()}</lastmod>`:''}</url>`).join('\n')}\n</urlset>`;}
export function robotsTxt(){const base=(process.env.SITE_URL||'https://avnture.example').replace(/\/$/,'');return `User-agent: *\nAllow: /\nDisallow: /admin\nDisallow: /api/\n\nSitemap: ${base}/sitemap.xml\n`;}
