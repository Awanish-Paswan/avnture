import { Router } from 'express'; import { body,param } from 'express-validator'; import { createContentController } from '../controllers/contentController.js'; import { requireAuth,requireRole } from '../middleware/auth.js'; import { BlogPost } from '../models/BlogPost.js'; import { Project } from '../models/Project.js'; import { sanitizeBody,validate } from '../middleware/validate.js';
function routerFor(Model,label){const router=Router();const c=createContentController(Model,label);router.get('/',async(req,res,next)=>{if(req.query.admin==='true')return requireAuth(req,res,()=>c.list(req,res,next));return c.list(req,res,next);});router.get('/:slug',[param('slug').isSlug()],validate,c.getBySlug);router.post('/',requireAuth,requireRole('admin','editor'),sanitizeBody,[body('title').isLength({min:3,max:160})],validate,c.create);router.put('/:id',requireAuth,requireRole('admin','editor'),sanitizeBody,[param('id').isMongoId()],validate,c.update);router.delete('/:id',requireAuth,requireRole('admin'),[param('id').isMongoId()],validate,c.remove);return router;}
export const projectRouter=routerFor(Project,'Project'); export const blogRouter=routerFor(BlogPost,'Blog post');
