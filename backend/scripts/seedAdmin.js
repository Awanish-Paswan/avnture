import bcrypt from 'bcryptjs'; import { connectDatabase } from '../config/db.js'; import { User } from '../models/User.js';
async function run(){const {ADMIN_NAME,ADMIN_EMAIL,ADMIN_PASSWORD}=process.env;if(!ADMIN_NAME||!ADMIN_EMAIL||!ADMIN_PASSWORD)throw new Error('Set ADMIN_NAME, ADMIN_EMAIL and ADMIN_PASSWORD before seeding.');if(ADMIN_PASSWORD.length<12)throw new Error('ADMIN_PASSWORD must contain at least 12 characters.');await connectDatabase();const passwordHash=await bcrypt.hash(ADMIN_PASSWORD,12);await User.findOneAndUpdate({email:ADMIN_EMAIL.toLowerCase()},{name:ADMIN_NAME,email:ADMIN_EMAIL.toLowerCase(),passwordHash,role:'admin'},{upsert:true,new:true,runValidators:true});console.log(`Admin account ready for ${ADMIN_EMAIL}.`);process.exit(0);}run().catch(e=>{console.error(e.message);process.exit(1);});
